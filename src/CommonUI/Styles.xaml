<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SharedUI"
                    xmlns:matrixPreview="clr-namespace:SharedUI.MatrixPreview">
    <DataTemplate x:Key="MatrixModelRemoveCellTemplate" DataType="matrixPreview:MatrixPreviewModel">
        <Button MaxWidth="100" MaxHeight="70" Command="{Binding RemoveCommand}" CommandParameter="{Binding}">Remove</Button>
    </DataTemplate>
</ResourceDictionary>