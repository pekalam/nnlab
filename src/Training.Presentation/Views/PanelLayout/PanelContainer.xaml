<UserControl x:Class="Training.Presentation.Views.PanelLayout.PanelContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Training.Presentation.Views.PanelLayout"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Border BorderThickness="1.5" Background="#F0F0F0" Cursor="Hand" MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#107EE5"/>

                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentControl x:Name="region"/>
        </Border>
    </Grid>
</UserControl>
