<UserControl x:Class="Training.Presentation.Views.Training.ActionMenuLeftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Training.Presentation.Views.Training"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:dd="clr-namespace:Training.Presentation.__DesignData"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             d:DesignHeight="450" d:DesignWidth="800"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance dd:DesignTrainingVm, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <Style TargetType="MenuItem" x:Key="CollapsingMenuItem">
            <Style.Triggers>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">
        <Button Style="{StaticResource ActionMenuButton}">
            <Grid>
                <mah:DropDownButton Content="{x:Null}" Width="66" Height="60">
                    <mah:DropDownButton.Style>
                        <Style TargetType="mah:DropDownButton">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="ArrowVisibility" Value="Collapsed" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="BorderBrush" Value="{x:Null}" />
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>
                    </mah:DropDownButton.Style>
                    <mah:DropDownButton.ItemsSource>
                        <CompositeCollection>
                            <MenuItem Header="Reset parameters" Command="{Binding Service.ResetParametersCommand}" />
                        </CompositeCollection>
                    </mah:DropDownButton.ItemsSource>
                </mah:DropDownButton>
                <StackPanel>
 
                    <iconPacks:PackIconFontAwesome Kind="ProjectDiagramSolid" Width="20" Height="20" HorizontalAlignment="Center"/>
                    <TextBlock HorizontalAlignment="Center">Network</TextBlock>
                </StackPanel>
  
            </Grid>

        </Button>
        <Button Style="{StaticResource ActionMenuButton}" Command="{Binding Service.OpenReportsCommand}" Margin="0,0,6,0">
            <StackPanel>
                <iconPacks:PackIconFontAwesome Kind="FileSolid" Width="20" Height="20" HorizontalAlignment="Center"/>
                <TextBlock>Reports</TextBlock>
            </StackPanel>
        </Button>
        <Button Style="{StaticResource ActionMenuButton}" Command="{Binding Service.OpenParametersCommand}">
            <StackPanel Margin="0,0,6,0">
                <iconPacks:PackIconFontAwesome Kind="SlidersHSolid" Width="20" Height="20" HorizontalAlignment="Center"/>
                <TextBlock>Parameters</TextBlock>
            </StackPanel>
        </Button>
        <Button Style="{StaticResource ActionMenuButton}">


            <Grid>
                <mah:DropDownButton Name="TestDropdown" Content="{x:Null}" IsEnabled="{Binding CanRunTests}" Width="66" Height="60" Margin="0,-8,0,0">
                    <mah:DropDownButton.Style>
                        <Style TargetType="mah:DropDownButton">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="ArrowVisibility" Value="Collapsed" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="BorderBrush" Value="{x:Null}" />
                            <Setter Property="Background" Value="Transparent"/>

                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <StackPanel>

                                            <iconPacks:PackIconFontAwesome Kind="CheckSolid" Width="20" Height="20" HorizontalAlignment="Center"/>
                                            <TextBlock TextAlignment="Center" FontSize="13">Tests</TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </mah:DropDownButton.Style>
                    <mah:DropDownButton.ItemsSource>
                        <CompositeCollection>
                            <MenuItem Name="ValidationMenuItem" Header="Validation set" Command="{Binding Service.RunValidationCommand}" />
                            <MenuItem Name="TestMenuItem" Header="Test set" Command="{Binding Service.RunTestCommand}" />
                        </CompositeCollection>
                    </mah:DropDownButton.ItemsSource>
                </mah:DropDownButton>


            </Grid>



        </Button>
    </StackPanel>
</UserControl>
