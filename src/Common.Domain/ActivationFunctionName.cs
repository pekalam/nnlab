namespace Common.Domain
{
    public enum ActivationFunctionName
    {
        Linear,
        Sigmoid,
        TanH,
        ArcTan
    }

    public enum ParamsInitMethod
    {
        NormalDist,
    }
}